#include<stdio.h>
int main ()
{
 int a[100],i,n;
 printf("enter the size");
 scanf("%d",&n);
 printf("enter the array elements");
 for(i=0;i<n;i++)
  {
   scanf("%d",&a[i]);
   }
   printf("reverse array");
   for(i=n-1;i>=0;i--)
   {
     printf("%d\t",a[i]);
   }
}   
